{
  "version": "1.0",
  "hooks": {
    "PreToolUse": [
      {
        "type": "command",
        "tool": "editFiles",
        "description": "Warn when writing implementation code outside ./build/ in a pipeline project.",
        "command": "bash .github/hooks/check-build-boundary.sh"
      }
    ]
  }
}
