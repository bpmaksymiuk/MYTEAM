{
  "name": "otel-cookbook-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --config=playwright.config.mjs --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
