{
  "name": "glagolitic-converter-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.mjs --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
